# for comparing and getting similarity score
from z3 import Probe, BoolRef, ArithRef, Or, And, Not, Solver

# calculate similarity between path contraints
def similarity(reference: BoolRef, student: BoolRef) -> float:
  
  return 0

# simplify boolean expression
def simplify(expr: BoolRef) -> BoolRef:
  
  return expr
